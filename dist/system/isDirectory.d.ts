/**
 * Checks if a path is a directory.
 * @param {string} dirPath - The path to check.
 * @returns {Promise<boolean>} True if the path is a directory, false otherwise.
 */
export function isDirectory(dirPath: string): Promise<boolean>;
